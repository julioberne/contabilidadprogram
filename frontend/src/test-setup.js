/* ============================================================
   test-setup.js — Setup global para Vitest + React Testing Library.
   Importa matchers de @testing-library/jest-dom para expect().
   ============================================================ */
import '@testing-library/jest-dom';
