# SPEC 09.F — SMS de Bancolombia → borradores automáticos del bot

> **Módulo:** 09 Bot IA · **Nivel:** etapa · **Estado:** EN CURSO (22 Sep 2026)
> **Versión:** 1.0 — 22 Sep 2026 · **Depende de:** [SPEC.md](SPEC.md) · **Reglas que aplican:** 2, 6, **6b**, 7
> **Qué NO cubre:** completar tercero/concepto desde Telegram (09.G), OCR de comprobantes (09.H), correo y notificaciones push (descartados: solo SMS), producción con HTTPS (prerrequisito externo: dominio + TLS en Dokploy).

## 1. Objetivo
Cada SMS de alerta de Bancolombia (remitente **85540**) que llega al teléfono de Andrés se convierte, sin tocar nada, en un **borrador** del bot de Telegram con monto, fecha, cuenta origen (por id) y, si el destino es un celular registrado, el tercero. Andrés lo completa y confirma como cualquier borrador. Nada se registra solo; nada se pierde.

## 2. Alcance
**Incluye:** app de automatización en Android (MacroDroid) → webhook autenticado → cola en `bot_messages` → parser por familias (regex) → `transaction_drafts` → Telegram; tokens por usuario; columnas `last4_cuenta` / `last4_tarjeta` en `user_accounts` editables desde 💳 Cuentas; retención 30/60/90; sistema de prueba local (simulador + túnel temporal).
**Excluye:** familias sin muestra real (Compraste, Retiraste, Recibiste, Pagaste: se agregan cuando Andrés envíe ejemplos; mientras tanto caen en "no reconocido"); `third_party_accounts` para cuentas bancarias de terceros (09.G); sugerencia de concepto (09.G).

## 3. Requisitos
| ID | Requisito | Origen |
|---|---|---|
| R-09F-01 | El webhook acepta solo peticiones con token válido por usuario, remitente permitido y cuerpo ≤ 4 KB; responde en < 1 s sin parsear ni enviar a Telegram. | Regla 7, D-09-01 |
| R-09F-02 | Un mismo SMS reenviado por la app no genera dos borradores (dedupe por `(channel, external_message_id)`). | índice existente `uq_bot_messages_in` |
| R-09F-03 | El parser es una tabla de familias con regex; sin LLM. Tolera año de 2 y 4 dígitos, montos `11,900.00` / `4,530,000` / `1.234,56`, `$`/`COP`/`USD`. | Muestras reales 21-sep-2026 |
| R-09F-04 | SMS no reconocido → borrador con `raw_text`, sin monto ni concepto (no confirmable) y encabezado "⚠️ SMS no reconocido". | Regla 6b |
| R-09F-05 | La cuenta origen se resuelve **por id** vía `user_accounts.last4_cuenta` (o `last4_tarjeta`). 0 o >1 coincidencias → borrador sin cuenta, no confirmable, con aviso. Nunca se cruza por nombre ni se asume "Efectivo". | D-09-03, Regla 6b |
| R-09F-06 | El tercero destino se resuelve solo por `third_parties.phone` cuando el destino es un celular (10 dígitos, empieza por 3) y hay exactamente una coincidencia. | Regla 6b |
| R-09F-07 | Si el destino coincide con `last4_cuenta` de otra cuenta propia → tipo `TRANSFERENCIA` con `dest_account_id`; si no, `GASTO` marcado como inferido. | determinista |
| R-09F-08 | La confirmación usa `payload.account_id` cuando existe (y la cuenta sigue existiendo); solo si falta resuelve por nombre como antes. | D-09-03 |
| R-09F-09 | Un error al procesar un SMS marca esa fila (`kind='sms_error'`), avisa al chat y no bloquea las demás; el poller jamás muere por el tick. | Regla 6b |
| R-09F-10 | Retención: `DESCARTADO` > 30 días se borra; `BORRADOR`/`ERROR` > 60 días → `DESCARTADO` con aviso; `bot_messages` > 90 días sin borrador vivo se borra; `CONFIRMADO` jamás. Kill-switch `BOT_RETENCION_ACTIVA=0`. | Regla 6b |
| R-09F-11 | El teléfono solo envía a una URL HTTPS (túnel temporal en pruebas; dominio + TLS en producción). | Regla 7 |

## 4. Diseño
**Flujo**
```
MacroDroid (SMS de 85540) ──POST /api/webhooks/sms  X-SMS-Token──▶ routers/webhooks_sms.py
   valida token/remitente/tamaño/tasa → INSERT bot_messages(channel='sms', kind='sms', draft_id NULL) → 202
poller bot_telegram.py (cada vuelta, ≤ 45 s) ──▶ bot_sms.procesar_pendientes(send_message)
   parsear (regex) → resolver cuenta por last4 (id) → tercero por celular → build_payload (sin LLM)
   → INSERT transaction_drafts(channel='sms') → UPDATE bot_messages.draft_id → commit
   → Telegram: encabezado 📲 + render_summary + _botones_borrador → bot_summary_message_id
```
**Datos**
- Nueva `sms_ingest_tokens` → `docs/database_schema.md` §T. Una fila por teléfono: `hub_user_id`, `token_hash` (sha256; el token plano se muestra una sola vez), `sender_allowlist` (default `{85540}`), `revoked_at`.
- `user_accounts` + `last4_cuenta VARCHAR(4)`, `last4_tarjeta VARCHAR(4)` (nulas; editables en 💳 Cuentas; backfill desde el nombre solo si contiene exactamente un grupo de 4 dígitos).
- Reutiliza `bot_messages` (`channel='sms'`; `kind` ∈ `sms` pendiente · `sms_sin_chat` sin link Telegram activo · `sms_error` falló · `retencion_aviso` salida de la purga; `raw_chat_id = hub_user_id`; `external_message_id = sha256(from|text|sentStamp)[:32]`; `content` = JSON del SMS) y `transaction_drafts` (`channel='sms'`, `raw_text` = SMS completo, `payload.sms = {familia, remitente, origen_last4, destino, hora}` para auditoría).
**Endpoints** (`docs/api_spec.md` §5): `POST /api/webhooks/sms` (token) · `POST /api/webhooks/sms/token`, `GET /api/webhooks/sms/tokens`, `DELETE /api/webhooks/sms/tokens/{id}` (sesión).
**Archivos**
| Acción | Archivo | Qué |
|---|---|---|
| CREAR | `fin_sys_core/sms_bancolombia.py` | parser puro: `FAMILIAS`, `parsear`, `normalizar_monto`, `normalizar_fecha`, `es_celular` |
| CREAR | `fin_sys_core/bot_sms.py` | DDL + `init_sms_tables`, `clave_dedupe`, resolvedores por id, `mapear_a_parsed`, `construir_borrador_sms`, `encabezado_sms`, `procesar_pendientes` |
| CREAR | `fin_sys_core/bot_retencion.py` | `purgar(send_fn)` |
| CREAR | `routers/webhooks_sms.py` | webhook + gestión de tokens |
| MODIFICAR | `server.py` | registrar router; `init_sms_tables()` en `_startup` |
| MODIFICAR | `fin_sys_core/bot_telegram.py` | tick SMS cada vuelta; purga cada hora |
| MODIFICAR | `fin_sys_core/bot_driver.py` | `_ejecutar_confirmacion`: cuenta por `account_id` primero (R-09F-08) |
| MODIFICAR | `routers/schemas.py`, `routers/profile_accounts.py`, `fin_sys_core/dashboard_query.py`, `frontend/.../CuentasTab.jsx` | `last4_cuenta` / `last4_tarjeta` |
| MODIFICAR | `fin_sys_core/storage_media.py` | `eliminar_evidencia` (best-effort) |
| CREAR | `scripts/migrate_sms_bancolombia.py`, `scripts/sms_token.py`, `scripts/sms_simular.py` | migración `--dry-run`, token por CLI, simulador de MacroDroid |
| MODIFICAR | `docker-compose.yml`, `.env.production.example`, `.github/workflows/ci.yml` | `BOT_RETENCION_ACTIVA`; tests puros nuevos |
**Seguridad:** token aleatorio de 32 bytes (`secrets.token_urlsafe`), guardado solo como sha256, comparado con `hmac.compare_digest`; allowlist de remitentes por token; cuerpo ≤ 4096 bytes (413); ≥ 60 SMS/min → 429; el texto del SMS se guarda como dato financiero personal bajo la misma auth de la bandeja; nada de esto viaja por HTTP plano (R-09F-11).

## 5. Decisiones
| ID | Decisión | Alternativas descartadas | Por qué |
|---|---|---|---|
| D-09F-01 | Envía el poller, no FastAPI. | `sendMessage` desde el request | D-09-01: un solo dueño del token; cola en Postgres; latencia ≤ 45 s aceptable. |
| D-09F-02 | Identidad por tabla `sms_ingest_tokens` (un token por teléfono, revocable, con allowlist). | Dos variables de entorno | Andrés 22-sep: multiusuario y sin redeploy para rotar. |
| D-09F-03 | Cuenta origen por `last4_cuenta`/`last4_tarjeta` (columnas nuevas, por id). | Buscar los 4 dígitos dentro de `name` | Andrés 22-sep: "trabajar por id, el nombre se cambia libremente". |
| D-09F-04 | El borrador SMS se arma con `build_payload` + resolvedores propios, **no** con `build_draft`. | Reutilizar `build_draft` con `texto=""` | `build_draft` pisa la fecha con hoy y `resolver_metodo_pago`/`resolver_cuenta` adivinan por palabras ("Bancolombia", "Efectivo"). |
| D-09F-05 | `ERROR` se trata como `BORRADOR` en la retención (60 días). | Excluirlo | Es un borrador que no pudo confirmarse; sin regla quedaría para siempre. |
| D-09F-06 | Pruebas con túnel `cloudflared` temporal al PC; producción solo con dominio + TLS. | HTTP plano a la IP de prod | El token y los SMS bancarios viajarían en claro. |
| D-09F-07 | Dos SMS idénticos en el mismo minuto se funden (misma clave de dedupe). | Incluir `receivedStamp` | La app puede reintentar; preferimos perder un duplicado raro a duplicar borradores. |

## 6. Criterios de aceptación
| ID | Criterio | Cómo se verifica | Estado |
|---|---|---|---|
| CA-09F-01 | Las 3 muestras reales parsean a 11900.0 / 2500.0 / 4530000.0, origen `3037`, fechas `2026-09-21`, destinos correctos, celular detectado. | `tests/test_sms_bancolombia.py` | ☐ |
| CA-09F-02 | Texto sin familia → `parsear` devuelve `None` y el borrador nace sin monto/concepto con encabezado ⚠️. | `tests/test_sms_bancolombia.py`, `tests/test_bot_sms.py` | ☐ |
| CA-09F-03 | Con `last4_cuenta='3037'` en una sola cuenta el borrador trae `account_id` de esa cuenta y `payment_method` = su nombre actual; con 0 o 2 coincidencias, `account_id=None`, `missing_fields` incluye `cuenta`. | `tests/test_bot_sms.py` | ☐ |
| CA-09F-04 | Destino celular con un tercero de `phone` igual → `third_party` con sus datos; sin coincidencia → "Sin especificar" inferido. | `tests/test_bot_sms.py` | ☐ |
| CA-09F-05 | Webhook: sin token 401, token inválido 401, remitente fuera del allowlist 403, > 4 KB 413, JSON y form-urlencoded 202, duplicado 202 `DUPLICADO` sin insertar. | `tests/test_webhooks_sms.py` | ☐ |
| CA-09F-06 | Un SMS pendiente en BD produce un borrador `channel='sms'` y un envío a Telegram; una segunda pasada no duplica; una fila envenenada queda `sms_error` y no bloquea. | `tests/test_bot_sms_db.py` (local) | ☐ |
| CA-09F-07 | Confirmar un borrador SMS crea la transacción con el `account_id` resuelto aunque la cuenta haya cambiado de nombre. | `tests/test_bot_sms_db.py` + E2E manual | ☐ |
| CA-09F-08 | Retención: reglas 30/60/90 y `CONFIRMADO` intacto; `BOT_RETENCION_ACTIVA=0` no toca nada. | `tests/test_bot_retencion.py` (local) | ☐ |
| CA-09F-09 | E2E local: `scripts/sms_simular.py` → 4 borradores en el chat de dev en ≤ 45 s; el de $11.900 se confirma; el no reconocido rechaza la confirmación por campos faltantes. | captura + Libro Diario | ☐ |
| CA-09F-10 | Un SMS real del teléfono, vía túnel HTTPS y MacroDroid, produce el borrador; MacroDroid ve 202. | captura | ☐ |

## 7. Plan de pruebas
- **Unitarias (CI):** `tests.test_sms_bancolombia`, `tests.test_bot_sms`, `tests.test_webhooks_sms` (añadidos a `ci.yml`).
- **Integración (local, BD compartida, `skipUnless`):** `tests.test_bot_sms_db`, `tests.test_bot_retencion` — limpian sus filas en `tearDown`, jamás crean transacciones salvo el test de confirmación, que elimina la suya.
- **Manual / E2E (PowerShell 5.1):**
  1. `.venv\Scripts\python.exe -m unittest tests.test_sms_bancolombia tests.test_bot_sms tests.test_webhooks_sms -v`
  2. `.venv\Scripts\python.exe scripts\migrate_sms_bancolombia.py --dry-run` → sin flag.
  3. Web → 💳 Cuentas → editar "Bancolombia Ahorros 3037 julian" → últimos 4 de la cuenta `3037`. Opcional: tercero con celular `3193301184`.
  4. Terminal 1: `.venv\Scripts\python.exe server.py` · Terminal 2: `.venv\Scripts\python.exe fin_sys_core\bot_telegram.py` (bot de dev).
  5. `.venv\Scripts\python.exe scripts\sms_token.py andres@finsys.os "Moto"` → copiar el token.
  6. `.venv\Scripts\python.exe scripts\sms_simular.py --token TOKEN` → 4 borradores en Telegram + `DUPLICADO`.
  7. ✅ Confirmar el de $11.900 → transacción con la cuenta correcta; intentar confirmar el no reconocido → "Faltan campos".
  8. `.venv\Scripts\python.exe -m unittest tests.test_bot_sms_db tests.test_bot_retencion -v`
  9. Túnel: `cloudflared tunnel --url http://localhost:8000` → MacroDroid (trigger *SMS Received* de `85540` → *HTTP Request* POST form-urlencoded `from={sms_number}`, `text={sms_message}`, header `X-SMS-Token`) → SMS real.

## 8. Operación
- **Variables:** `BOT_RETENCION_ACTIVA` (servicio `bot`, default `1`). El backend no necesita variables nuevas (tokens y allowlist en BD).
- **Migración:** `scripts/migrate_sms_bancolombia.py` desde el PC **antes** del deploy (crea `sms_ingest_tokens`, añade `last4_*`, backfill prudente). El server también auto-cura la tabla y las columnas en `_startup()`.
- **Deploy / rollback:** push a `master` → Dokploy. Apagar: revocar el token (`DELETE /api/webhooks/sms/tokens/{id}`) o desactivar la macro; `BOT_RETENCION_ACTIVA=0` detiene la purga. Ninguna tabla existente se altera de forma destructiva.
- **Retención / límites:** R-09F-10; ≤ 60 SMS/min por instalación; un SMS pesa ~300 B en `bot_messages` + ~1 KB en `transaction_drafts`.
- **Prerrequisito de producción:** dominio + certificado en Dokploy/Traefik (hoy `http://159.223.156.50:8080` es HTTP plano). Hasta entonces el teléfono solo apunta al túnel de pruebas.

## 9. Checklist de cierre
- [ ] `CA-09F-*` marcados y `Estado: HECHO (fecha)`
- [ ] `CHECKLIST.md` fila 09 + comando de tests
- [ ] `docs/checkpoints.md`
- [ ] `docs/database_schema.md` (§T `sms_ingest_tokens`, columnas `last4_*` en `user_accounts`) y `docs/api_spec.md` (§5 webhooks)
- [ ] `SPEC.md` fila F → ✅
